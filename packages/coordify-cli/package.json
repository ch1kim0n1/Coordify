{
  "name": "coordify-cli",
  "version": "0.1.0",
  "private": true,
  "description": "Coordify CLI — query, watch, graph",
  "main": "dist/cli.js",
  "bin": {
    "coordify": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc && tsc -p tsconfig.tui.json && node -e \"require('fs').cpSync('src/tui/package.json','dist/tui/package.json')\"",
    "test": "npx tsx --test 'test/**/*.test.ts' 'test/**/*.test.tsx'"
  },
  "dependencies": {
    "ink": "^4.4.1",
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/react": "^18.2.0",
    "ink-testing-library": "^4.0.0",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
